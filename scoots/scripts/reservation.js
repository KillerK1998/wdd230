document.getElementById('reservationForm').addEventListener('submit', function(event) {
    // Perform any additional actions if necessary
    // For example, form validation
    // If validation passes, do not prevent default behavior
    // Otherwise, call event.preventDefault() to stop submission
});
